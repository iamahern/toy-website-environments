name: deploy-toy-website-environments
concurrency: toy-company

on: 
  push:
    branches: 
      - main
  workflow_dispatch: {}


jobs:

  # Lint the Bicep file.
  lint:
    uses: iamahern/toy-website-environments/.github/workflows/lint.yml@main
